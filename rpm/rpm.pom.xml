<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
	 xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	 xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <packaging>rpm</packaging>
  <modelVersion>4.0.0</modelVersion>
  <artifactId>kaf_04</artifactId>
  <version>${revision}${sha1}${changelist}</version>
  <name>kaf_04</name>
  <description>kaf_04</description>
  <groupId>com.teragrep</groupId>
  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <revision>0.0.1</revision>
    <changelist>-SNAPSHOT</changelist>
    <sha1/>
  </properties>
  <build>
    <directory>${project.basedir}/target</directory>
    <plugins>
      <plugin>
        <artifactId>maven-enforcer-plugin</artifactId>
        <version>3.4.1</version>
        <executions>
          <execution>
            <id>enforce</id>
            <phase>none</phase>
          </execution>
          <execution>
            <id>enforce-maven</id>
            <goals>
              <goal>enforce</goal>
            </goals>
            <configuration>
              <rules>
                <requireMavenVersion>
                  <version>3.2.5</version>
                </requireMavenVersion>
              </rules>
            </configuration>
          </execution>
        </executions>
        <configuration>
          <rules>
            <requireJavaVersion>
              <version>1.8</version>
            </requireJavaVersion>
          </rules>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>rpm-maven-plugin</artifactId>
        <version>2.3.0</version>
        <extensions>true</extensions>
        <executions>
          <execution>
            <id>default-rpm</id>
            <goals>
              <goal>rpm</goal>
            </goals>
            <phase>package</phase>
          </execution>
        </executions>
        <configuration>
          <summary>${project.groupId}-${project.artifactId}</summary>
          <name>${project.groupId}-${project.artifactId}</name>
          <version>${project.version}</version>
          <release>${env.BUILD_ID}</release>
          <license>Proprietary</license>
          <distribution>Teragrep kaf_04</distribution>
          <vendor>https://teragrep.com/</vendor>
          <packager>Teragrep &lt;servicedesk@teragrep.com&gt;</packager>
          <group>Teragrep/kaf_04</group>
          <description>${project.groupId}-${project.artifactId}</description>
          <needarch>false</needarch>
          <defaultUsername>root</defaultUsername>
          <defaultGroupname>root</defaultGroupname>
          <defaultFilemode>0644</defaultFilemode>
          <defaultDirmode>0755</defaultDirmode>
          <keyname>${env.RPM_SIGNING_KEYNAME}</keyname>
          <keyPassphrase>
            <passphrase>${env.RPM_SIGNING_PASSPHRASE}</passphrase>
          </keyPassphrase>
          <mappings>
            <mapping>
              <directory>/opt/teragrep/${project.artifactId}/share</directory>
              <directoryIncluded>true</directoryIncluded>
              <filemode>755</filemode>
              <dirmode>755</dirmode>
              <username>root</username>
              <groupname>root</groupname>
              <recurseDirectories>true</recurseDirectories>
              <sources>
                <source>
                  <location>${project.basedir}/../target/kaf_04-jar-with-dependencies.jar</location>
                  <destination>kaf_04.jar</destination>
                </source>
              </sources>
            </mapping>
            <mapping>
              <directory>/opt/teragrep/${project.artifactId}/etc</directory>
              <configuration>noreplace</configuration>              
              <directoryIncluded>true</directoryIncluded>
              <filemode>640</filemode>
              <dirmode>750</dirmode>
              <username>root</username>
              <groupname>srv-kaf_07</groupname>
              <recurseDirectories>true</recurseDirectories>
              <sources>
                <source>
                  <location>${project.basedir}/src/main/resources/authorize.example.json</location>
                </source>
                <source>
                  <location>${project.basedir}/src/main/resources/cluster.example.json</location>
                </source>
                <source>
                  <location>${project.basedir}/src/main/resources/writer.example.json</location>
                </source>
                <source>
                  <location>${project.basedir}/src/main/resources/identitySuffix.example.json</location>
                </source>
              </sources>
            </mapping>
          </mappings>
          <requires>
            <require>java-1.8.0-openjdk</require>
          </requires>
          <preinstallScriptlet>
            <script>
getent group srv-kaf_07 > /dev/null || groupadd -r srv-kaf_07;
getent passwd srv-kaf_07 > /dev/null || useradd -r -g srv-kaf_07 -d /opt/teragrep/${project.artifactId} -s /sbin/nologin -c "Service User" srv-kaf_07;
exit 0;
            </script>
          </preinstallScriptlet>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>
